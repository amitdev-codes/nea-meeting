<div class="text-center mb-4">
    <!-- Government Logo -->
    <img src="{{ asset('assets/img/nea-logo.png') }}" alt="Government Logo" class="mb-3" width="60">

    <!-- Project Title -->
    <!-- Middle: Project Title -->
    <div class="text-center flex-grow-1 mb-4">
        {{-- <h4 class="text-uppercase card-header mb-1 fw-bold text-danger">{{ __('landing.government of nepal') }}</h3> --}}
        <h4 class="card-header mb-1 fw-bold text-danger">
            {{ __('landing.nepal electricity authority') }}</h4>
        <h5 class="card-header mb-1 fw-bold text-success">
            {{ __('landing.meeting management system') }}</h5>
    </div>
</div>
